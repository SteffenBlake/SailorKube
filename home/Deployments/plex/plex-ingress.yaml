apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plex-ingress
  namespace: plex-system
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: plex.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: plex-svc
            port:
              number: 32400